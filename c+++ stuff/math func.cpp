#include <iostream>
#include <cmath>

int main(int argc, char const *argv[])
{
    double x = 4.56;
    double y = 6;
    double z;   

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(2 , 300);
    //z = sqrt(0);
    //z = abs(-3);
    //z = round(x);
    //z = ceil(x);
    //z = floor(x);
    std::cout << z;

    return 0;
}